{
  "name": "anticus-tests",
  "version": "0.0.0",
  "private": true,
  "description": "Тесты для статического приложения. Само приложение зависимостей не имеет и открывается как index.html.",
  "scripts": {
    "test": "node --test \"tests/**/*.test.js\"",
    "test:watch": "node --test --watch \"tests/**/*.test.js\""
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
